# CI-safe defaults for integration/e2e tests.
# Copy this file to `.env.test` to override locally (file is gitignored).

# Local onchain-actions dev server (default is https://api.emberai.xyz)
ONCHAIN_ACTIONS_BASE_URL=http://localhost:50051
